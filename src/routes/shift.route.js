import express from "express";

const shiftsRoute = express.Router();

shiftsRoute.get("/", () => {});
shiftsRoute.post("/", () => {});
shiftsRoute.put("/:id", () => {});

export default shiftsRoute;
